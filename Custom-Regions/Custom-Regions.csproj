<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">

  <!--Settings-->
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>

    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <ProjectGuid>{13837869-9236-4D05-8841-E265AB15028C}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>CustomRegions</RootNamespace>
    <AssemblyName>CustomRegions</AssemblyName>
    <TargetFramework>net35</TargetFramework>
    <Deterministic>true</Deterministic>
  </PropertyGroup>

  <!--Debug configuration-->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>

  <!--Release configuration-->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>

  <!--References & items-->
  <PropertyGroup>
    <ReferencePath>C:\Rain World References</ReferencePath>
  </PropertyGroup>
  <!--Add a 'user.targets' file to the repository and set the ReferencePath property to whatever you want there to change where your references go.
      If your references are contained within the repository, make sure to gitignore them.-->

  <!--EXAMPLE
<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <ReferencePath>C:\MyReferencePathGoesHere</ReferencePath>
  </PropertyGroup>
</Project>
-->
  <!--Nothing need be done if your references are already in 'C:\Rain World References' though.-->
  <Import Condition="Exists('user.targets')" Project="user.targets" />
  <ItemGroup>
    <Reference Include="$(ReferencePath)\Assembly-CSharp.dll" />
    <Reference Include="$(ReferencePath)\ConfigMachine.dll" />
    <Reference Include="$(ReferencePath)\EnumExtender.dll" />
    <Reference Include="$(ReferencePath)\HOOKS-Assembly-CSharp.dll" />
    <Reference Include="$(ReferencePath)\UnityEngine.dll" />
    <Reference Include="lib\MonoMod.exe" />
    <Reference Include="lib\MonoMod.RuntimeDetour.dll" />
    <Reference Include="lib\Partiality.dll" />
    <Reference Include="System" />
    <Reference Include="System.Core" />
    <Reference Include="System.Xml.Linq" />
    <Reference Include="System.Data.DataSetExtensions" />
    <Reference Include="System.Data" />
    <Reference Include="System.Xml" />
  </ItemGroup>
</Project>